  <div class="row">
	  	<div class="col-md-4">
	  		<%= image_tag item.picture.url(:medium), class: 'uploaded-pic, img-rounded'%>
	  		<p><%= item.description %></p>
	  		<p><%= item.price %></p>
	  		<p>
	  		<%= link_to 'Delete', post_path(post), method: :delete, class: 'btn btn-danger' %></p>
	  	</div>
  </div>